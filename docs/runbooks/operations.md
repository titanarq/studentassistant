# Operations runbook -- studentassistant agent OS

How to watch, pause and patch the agent mechanism (`agent_os/`, never edited here) on this host.
Background: `agent_os/docs/AGENT_OS.md` §2.4, §6, §8 and `.claude/agents/control-plane.md` Duty 5.

## Units (systemd --user)

| unit | what | every |
|---|---|---|
| `studentassistant-guard.timer` | `scripts/agent_guard.py tick`: budget/liveness/quota check, writes planner events, wakes the planner | 5 min |

The guard's units are generated by `agent-os-install` (no host drop-ins besides the installer's own
`override.conf`); after re-rendering them: `systemctl --user daemon-reload`. The board-sync timer
is gone (see "Workarounds removed with the 5a2da21 subtree pull").

## Monitor

```sh
systemctl --user list-timers --no-pager | grep studentassistant        # guard timer armed?
journalctl --user -u studentassistant-guard.service --since "1 hour ago" -o cat \
  | grep -vE "never started|^0 event|nothing to wake" | tail -40      # tick output
scripts/worker_progress.sh                                           # every backend, last hour
scripts/worker_progress.sh --hours 6 37 qwen                         # one issue / backend
cat .cache/worker_qwen.issue .cache/worker_qwen.state                # what the qwen worker holds
tail -5 .cache/planner/runs.tsv .cache/refiner/runs.tsv              # role runs + cost (USD)
scripts/issues.py list --label status:doing                          # likewise ready/review/blocked-on-human
gh pr list --state open
git -C ../studentassistant-qwen log --oneline -5                        # worker branch commits
```

Qwen reports no cost: report its spend in tokens (`worker_progress.sh`), never as 0 USD.
Alarms: an `EXPECT` past its `cutoff` with no later line; a Qwen issue past ~60 M tokens; a
`blocked-on-human` older than an hour; a timer inactive. Never `cat` a `.jsonl` stage log.

## Pause / resume

- Full stop (the tick returns before doing anything): put `status:agents-paused` on the tracking
  epic #1 -- `scripts/issues.py update 1 --add-label status:agents-paused`; remove it to resume.
  This is the human's lever. A running worker is not killed by it; it is cut at its next check.
- Stop ticking altogether: `systemctl --user disable --now studentassistant-guard.timer`
  (re-arm: `systemctl --user daemon-reload && systemctl --user enable --now studentassistant-guard.timer`).
- Wake the planner early: comment the reason, then `scripts/issues.py update N --add-label wake:planner`.

## CI runners: GitHub-hosted only (repo is public)

Since 2026-09-24 `titanarq/studentassistant` is a **public** repository, so GitHub-hosted Actions
minutes are free. **Every workflow in this repo uses `runs-on: ubuntu-latest`** (GitHub-hosted).
Never target `self-hosted` (any label) from this repo: a fork PR could run arbitrary code on the
human's machine. The org's Default runner group keeps `allows_public_repositories=false`, so a
self-hosted job here would queue forever anyway. Toolchains come from the hosted image or from
setup actions (`actions/setup-python`/`astral-sh/setup-uv`, `actions/setup-node`,
`actions/setup-java` with JDK 17; the image ships an Android SDK in `$ANDROID_HOME`).

Repo Actions settings for public-repo safety (keep them): fork PR workflows need approval for
all outside collaborators (`gh api repos/titanarq/studentassistant/actions/permissions/fork-pr-contributor-approval`
-> `all_external_contributors`) and the default `GITHUB_TOKEN` is read-only
(`.../actions/permissions/workflow` -> `default_workflow_permissions=read`).

The machine's local self-hosted runners belong to teachermovies; nothing in this repo may use them.
other-repo reference only; nothing in this repo may use them.

## GraphQL quota

The owner's GraphQL quota (5000 points/h) is shared with teachermovies and every `gh` call made
as MatillaM. **`gh api rate_limit`'s top-level `.rate` is the REST `core` bucket**; read
`.resources.graphql`, or `gh api graphql -f query='{rateLimit{remaining used resetAt cost}}'`.
Since the 36985fc subtree (agent-os#27) `issues.py move` costs ~3 GraphQL points whatever the
board size, and a batch `issues.py move 2 3 4 refine` resolves the label and board field once.
Since agent-os#70 a primary rate limit on an empty bucket exits at once naming it and its reset
time instead of retrying.

## Branch protection on `main`

`main` requires the `ci` status check (`strict=false`: a PR need not be up to date with `main`),
no required reviews, and `enforce_admins=false`, so the human's direct pushes still work. Read it
with `gh api repos/titanarq/studentassistant/branches/main/protection` (REST).

## Known mechanism issues filed upstream

- agent-os#14, #18, #27, #32, #33, #37, #39, #41, #51, #52, #71, #75, #86: fixed upstream,
  workarounds removed (below).
- agent-os#15 refiner-created tasks miss the `[task] ` title prefix (fix titles by hand with
  `issues.py update N --title`).
- agent-os#16 `worker_task.sh start` rejects hyphenated branch prefixes such as `agent-os/37-...`;
  the planner retries with `task/<issue>-<slug>`.
- agent-os#17 (fixed upstream, PR #34): its two `--deselect`s in `ci-agent-os.yml` are gone.
- agent-os#82 (fixed upstream at 4319091): `test_no_host_literals` no longer needs a `.git` under
  `agent_os/`; its two `--deselect`s in `ci-agent-os.yml` are gone.

## Refiner

`planner.refiner_unattended` is `true` on this host: the backlog was written for the refiner to
write every task's `## Stages` (and split what is too big), and the same mechanism version's dry
run was reviewed on teachermovies. Attended run by hand:
`bash agent_os/bin/agent_task.sh refiner N --no-wake` (detached; log in `.cache/refiner/`);
`--dry-run` only prints the resolved prompt. Set it to `false` in `config/agents.yaml` to stop
unattended refinement. The guard names refine-needing issues in critical-path order (agent-os#32),
the refiner gets its own `AGENT_RUN_SCRATCH` directory (agent-os#33) and closes a split task with
`issues.py supersede N --by A --by B`, which repoints every dependent's `Blocked by` (agent-os#39).

## Worker routing (Claude + Qwen)

**Since 2026-09-25 evening (the human: "puedes usar qwen y opus para los workers ya") both worker
backends take work**, one run each at a time (`max_parallel_issues: 2`):

| class | backend / model | ceilings | on a Claude quota cut |
|---|---|---|---|
| `mechanical-qwen` | qwen / `qwen3.8-max` (worktree `../studentassistant-qwen`) | 400k context, $5, 80M tokens | n/a |
| `complex-qwen` | claude / `claude-opus-5-5` (worktree `../studentassistant-claude`) | 800k, $20, 150M | redispatched on Qwen |
| `complex-claude` | claude / `claude-opus-5-5` | 800k, $20, 150M | redispatched on Qwen |

The class names are kept because issue bodies reference them. `mechanical-qwen` is the first
worker class on the qwen backend, so it also names the model a Qwen quota fallback launches with
(`agent_os.lib backend-model qwen`); the earlier placeholder class `qwen-fallback` is gone. Both
backends are declared under `project.backends` (`command`, `worktree`, `app`, `stream`, `quota`);
`project.executables` only names `gh`.
The config is read fresh from the main checkout's `config/agents.yaml` on every guard tick and
every `worker_task.sh` call, so a `git pull --ff-only` in the main checkout is the whole reload (no
`agent-os-install`, no daemon-reload): a run already alive keeps its backend, and only new
dispatches and relaunches pick up the new one.

Earlier 2026-09-25 (#239) every worker class ran on Claude Opus with Qwen only as the quota
fallback. Before that, since 2026-09-24, `planner.max_parallel_issues` is 2 and the class `complex-claude` (backend
claude, `qwen_fallback_eligible: true`) carries the harder critical-path backend tasks: every open
`complex-qwen` task in modules server, observer, editor, llm and protocol was retagged to it.
Everything else stays on `mechanical-qwen`/`complex-qwen`. The refiner's extras
(`config/agent_prompts/refiner.md`, updated with the human's authorization) route complex tasks in
those five modules to `complex-claude` and everything else to the Qwen classes. Every Claude class
and role runs `claude-opus-5-5` (human's decision, 2026-09-24).

## Starting the agents

Started 2026-09-24: the guard timer armed, `auto-ready` on epics #2, #3, #4 and #5 (M0/M1), and the refine
window parked (the window is gone since the 36985fc subtree; see below).

The guard timer is NOT armed by setup. Before arming it, put `auto-ready` on the epics whose
tasks may move to Ready for AI once refined (start with the M0/M1 epics), then:

```sh
systemctl --user daemon-reload
systemctl --user enable --now studentassistant-guard.timer
```

## Workarounds removed with the 36985fc subtree pull

`agent_os/` was pulled at titanarq/agent-os 36985fc (announced on titanarq/roedor#535), which fixes
these upstream; `agent_os/docs/CHANGELOG.md` has the details. Removed from this repo:

| upstream | was | why it can go |
|---|---|---|
| agent-os#27 | `scripts/agent_os_patches/` (interpreter wrapper + cheap board lookups) and the `AGENT_OS_PYTHON` default in the six `scripts/` shims | `issues.py move` does one bounded ~1-point query per lookup; the mechanism's own `agent_os/.venv/bin/python` is the interpreter again |
| agent-os#32 | `scripts/refine_window.py`, its `ExecStart=` in the (now removed) board-sync service, the `parked` label | `refine_pending` names a ranked queue (auto-ready parent, priority, unblocked, number) |
| agent-os#33 | `SCRATCH FILES` block in `config/agent_prompts/refiner.md` (human edits it) | each role run gets `AGENT_RUN_SCRATCH`; prompts forbid touching `.cache/` |
| agent-os#37 | `scripts/sanitize_role_logs.py` + guard drop-in `sanitize-role-logs.conf` | parsers accept a string `message` |
| agent-os#52 | `scripts/reset_stale_stall_bookkeeping.py` + guard drop-in `reset-stale-stall-bookkeeping.conf` | bookkeeping carries a `run_identity` and resets on a new run |
| agent-os#39 | manual repoint of dependents after a refiner split | the refiner calls `issues.py supersede` |
| agent-os#41 | re-running the validator with a "run `scripts/test.sh`" planner context | `project.worktree_setup_command` / `worktree_links` provision every worktree (`config/agents.yaml`, human edits it) |
| agent-os#51 | guard `ExecStart=` on the host `.venv` | the installer renders the mechanism's interpreter; re-render with `agent-os-install --force` + daemon-reload |

`scripts/test.sh` keeps provisioning itself (`uv sync --frozen` / `npm ci`): CI and hand-made lanes
rely on it, not only the validator.

Done on this host at the merge of #229 (2026-09-25): the guard drop-ins
`sanitize-role-logs.conf` and `reset-stale-stall-bookkeeping.conf` removed, `agent-os-install
--force` + daemon-reload.

Also: `.github/workflows/ci-agent-os.yml` is the template as-is (agent-os#17's and agent-os#82's
`--deselect`s dropped, both fixed upstream; path filter per upstream PR #79), so
`agent-os-install --force` leaves it unchanged; after a `--force` check it still has no
`--deselect`.

## Workarounds removed with the 5a2da21 subtree pull

`agent_os/` at titanarq/agent-os 5a2da21 (#229) also covers the last host workarounds; removed:

| upstream | was | why it can go |
|---|---|---|
| agent-os#14, #23 | `scripts/board_sync.py` + the `studentassistant-board-sync` timer/service (copied from `scripts/systemd/`) | `issues.py move` finds the board item from the issue's own `projectItems`; `issues.py create` adds new issues to the board; the guard reconciles closed issues to `done` |
| agent-os#18, #71, #75, #86 | `scripts/clean_stale_worker.sh`, `scripts/clean_stale_workers_if_closed.sh`, `scripts/tests/` and `ci.yml`'s `host-scripts` job | the driver writes `scratchpad/.gitignore` so leftovers never block a dispatch, archives a finished run's scratchpad, and `branch` re-branches from `origin/main` with `checkout -B`; real dirt pages the human (`project.messages.backend_worktree_dirty`) |
| agent-os#86 | hand-added `scratchpad/` lines in a worktree's `info/exclude` | the driver hides `scratchpad/` itself |
| agent-os#514 | `project.worktrees` / `worker_apps` (deprecated alias) | `project.backends.<name>` (already the shape of `config/agents.yaml`) |

On this host after merging: `systemctl --user disable --now studentassistant-board-sync.timer`,
then `rm ~/.config/systemd/user/studentassistant-board-sync.{service,timer}` and
`systemctl --user daemon-reload`.
