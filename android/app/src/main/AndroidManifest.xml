<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The pairing QR scanner (and, later, page capture). Requested at runtime. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The paired backend on the LAN. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!--
      allowBackup/dataExtractionRules: the paired backends' bearer tokens live in app-private
      storage and must not leave the device through cloud backup or device-to-device transfer.
      networkSecurityConfig: the backend is served over plain HTTP on the LAN.
    -->
    <application
        android:name=".StudentAssistantApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
